{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Genres creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('catalog_films_genres') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
